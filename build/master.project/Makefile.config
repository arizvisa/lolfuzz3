# Configurations containing the /srv to deploy with
repository_configuration := $(shell git rev-parse -q --verify "serve")
project_configuration := $(ROOTDIR)/config/$(project).project
branch_configuration := $(ROOTDIR)/srv

# List of static files that are required for deployment (does not include template)
output_names := $(boxname).config $(boxname).id_rsa $(boxname).id_rsa.pub
